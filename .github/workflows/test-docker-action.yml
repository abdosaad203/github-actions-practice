name: Test Docker Action (Q4)

on:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Docker Message Action
        uses: ./.github/actions/docker-message
